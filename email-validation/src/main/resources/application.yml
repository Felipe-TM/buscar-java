server:
   port: 8090

spring:
  profiles:
      active: dev
  application:
      name: EmailVerificationApp
               
secrets:
    name: Buscar-Catch-A-Ride
    baseUrl: http://localhost:8090/api/v1/validate